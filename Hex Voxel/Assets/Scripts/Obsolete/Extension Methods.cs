using UnityEngine;

public static class ExtensionMethods
{
    /*
    public static WorldPos ToWorldPos(this Vector3 value)
    {
        return new WorldPos(Mathf.RoundToInt(value.x), Mathf.RoundToInt(value.y), Mathf.RoundToInt(value.z));
    }
    */
}
